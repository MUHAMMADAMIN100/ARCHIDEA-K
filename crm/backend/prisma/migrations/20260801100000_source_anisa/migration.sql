-- Источник «От Анисы» отдельным пунктом списка.
--
-- ALTER TYPE ... ADD VALUE внутри транзакции допустим с PostgreSQL 12,
-- пока новое значение не используется в этой же транзакции — здесь только
-- объявление, поэтому миграция проходит одним файлом как обычно.
ALTER TYPE "LeadSource" ADD VALUE IF NOT EXISTS 'ANISA';
