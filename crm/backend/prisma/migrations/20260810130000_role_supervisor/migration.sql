-- Новая роль «Управляющий»: вся работа компании, кроме денег и доступов.
--
-- Значение перечисления добавляется ОТДЕЛЬНОЙ миграцией: PostgreSQL не
-- позволяет пользоваться свежим значением enum в той же транзакции, где оно
-- создано. Назначение роли людям — в следующей миграции.
ALTER TYPE "Role" ADD VALUE IF NOT EXISTS 'SUPERVISOR';
