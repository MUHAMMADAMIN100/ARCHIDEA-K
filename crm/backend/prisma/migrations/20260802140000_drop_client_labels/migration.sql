-- Свободные теги клиента убраны.
--
-- Их было два вида: фиксированный статус (VIP, Постоянный, Потенциальный,
-- Отказник) и произвольные строки. В списке клиентов показывались оба, в
-- остальных местах — только статус, и одно и то же состояние выглядело
-- по-разному на разных экранах. Остаётся один статус.
ALTER TABLE "Client" DROP COLUMN IF EXISTS "labels";
