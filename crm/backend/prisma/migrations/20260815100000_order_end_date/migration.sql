-- Уборка не всегда укладывается в один день (просьба владельца).
--
-- Дата окончания необязательна: пусто — работа на один день, как было
-- раньше. Ни одна существующая строка не меняется, поэтому выкат
-- безопасен и обратим — колонку можно просто удалить.
ALTER TABLE "Order" ADD COLUMN IF NOT EXISTS "scheduledEndDate" TIMESTAMP(3);
