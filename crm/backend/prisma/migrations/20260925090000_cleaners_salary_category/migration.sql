-- Статья расхода «ЗП клинеров» (просьба владельца, сентябрь 2026).
--
-- Выплаты бригадам, которые заносят в книгу руками, записывали в «Зарплату»
-- или «Прочий расход» — и они не попадали на плитку «ЗП клинеров» в
-- аналитике. Теперь у них своя статья: она считается на этой плитке вместе
-- с начислениями по сменам и не входит в «Все расходы».
--
-- ADD VALUE стоит в ОТДЕЛЬНОЙ миграции нарочно: PostgreSQL не разрешает
-- использовать новое значение перечисления в той же транзакции, где оно
-- добавлено. Существующие записи не меняются — только новый пункт в списке.
ALTER TYPE "FinanceCategory" ADD VALUE IF NOT EXISTS 'CLEANERS_SALARY';
