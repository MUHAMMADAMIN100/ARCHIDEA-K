-- Предупреждение за час до срока напоминания (решение владельца: слать дважды —
-- за час и в сам срок). Отметка нужна, чтобы предупреждение ушло один раз:
-- планировщик просыпается каждые пять минут.
ALTER TABLE "Reminder" ADD COLUMN IF NOT EXISTS "preNotifiedAt" TIMESTAMP(3);
