-- Статья расхода «Коммунальные услуги» (просьба владельца).
--
-- Свет, вода, интернет офиса записывали в «Прочий расход» или «Аренду» —
-- и в аналитике эти деньги смешивались с чужими статьями.
--
-- ADD VALUE стоит в ОТДЕЛЬНОЙ миграции нарочно: PostgreSQL не разрешает
-- использовать новое значение перечисления в той же транзакции, где оно
-- добавлено. Существующие записи не меняются — только новый пункт в списке.
ALTER TYPE "FinanceCategory" ADD VALUE IF NOT EXISTS 'UTILITIES';
